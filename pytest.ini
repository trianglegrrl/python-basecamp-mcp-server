[pytest]
# Live tests hit a real Basecamp 3 sandbox and require credentials in env.
# Excluded by default; opt in with `-m live` or `pytest tests/live/`.
markers =
    live: integration test requiring a real BC sandbox + creds
addopts = -m "not live"
asyncio_mode = auto
